# Listing 生成 Plugin — 环境变量配置

# PCE 引擎地址（生产部署后改为实际地址）
PCE_API_BASE=http://127.0.0.1:8180

# Flask 配置
FLASK_ENV=development
PORT=5001

# Listing Plugin 自身端点（供 MCP 注册回调使用）
LISTING_ENDPOINT=http://localhost:5001/api/v1/listing/generate
